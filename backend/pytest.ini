[pytest]
testpaths = tests
# Quiet by default. The interesting output of this suite is which test failed
# and why, not a list of everything that passed.
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
